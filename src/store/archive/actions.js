import {setArchive} from "./action-creators";

/*
export function getArchive () {
    return async dispatch => {
        try {
            const response = await axios(`URl_API`);
            dispatch(setArchive(response.data));
        }
        catch (e) {
            console.log(e?.response?.data?.message);
        }
    }
}*/
